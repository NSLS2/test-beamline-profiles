# Copilot Instructions

## Git workflow

Before making any modifications to a locally cloned repository, always pull the latest changes from the remote first:

```bash
git pull
```

This prevents working on stale code and avoids conflicts with changes already pushed to GitHub.
